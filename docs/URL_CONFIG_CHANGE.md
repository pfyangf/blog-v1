# 🔄 URL 路径配置变更说明

## 变更内容

已将 `baseUrl` 从 `/blog-v1/` 改为 `/`

### 修改前
```typescript
url: 'https://pfyangf.github.io',
baseUrl: '/blog-v1/',
```

### 修改后
```typescript
url: 'https://pfyangf.github.io',
baseUrl: '/',
```

---

## 📍 访问地址变化

### 本地开发
- **中文首页**: `http://localhost:3000/`
- **英文首页**: `http://localhost:3000/en/`
- **归档页面**: `http://localhost:3000/archive/`
- **标签云**: `http://localhost:3000/tags-cloud/`

### GitHub Pages 部署

**重要**: 由于改为根路径，你需要选择以下两种部署方式之一：

#### 选项 1: 使用自定义域名（推荐）

1. **购买域名**（如 `yourdomain.com`）

2. **配置 DNS**
   在域名提供商处添加 CNAME 记录：
   ```
   blog.yourdomain.com → pfyangf.github.io
   ```

3. **创建 CNAME 文件**
   在 `static/` 目录下创建 `CNAME` 文件：
   ```
   blog.yourdomain.com
   ```

4. **更新配置**
   修改 `docusaurus.config.ts`:
   ```typescript
   url: 'https://blog.yourdomain.com',
   baseUrl: '/',
   ```

5. **GitHub 设置**
   - 访问: https://github.com/pfyangf/blog-v1/settings/pages
   - Custom domain: 输入 `blog.yourdomain.com`
   - 勾选 "Enforce HTTPS"

6. **访问地址**
   - 中文: `https://blog.yourdomain.com/`
   - 英文: `https://blog.yourdomain.com/en/`

#### 选项 2: 使用 GitHub 用户页面

如果你想使用 `https://pfyangf.github.io/` 作为博客地址：

1. **重命名仓库**
   将仓库名从 `blog-v1` 改为 `pfyangf.github.io`

2. **访问地址**
   - 中文: `https://pfyangf.github.io/`
   - 英文: `https://pfyangf.github.io/en/`

#### 选项 3: 恢复 /blog-v1/ 路径

如果不想配置自定义域名，可以恢复原配置：
```typescript
baseUrl: '/blog-v1/',
```

---

## 🚀 测试步骤

### 1. 本地测试

```bash
# 清除缓存
npm run clear

# 启动中文版
npm start

# 启动英文版
npm run start -- --locale en
```

访问:
- 中文: http://localhost:3000/
- 英文: http://localhost:3000/en/

### 2. 构建测试

```bash
npm run build
```

检查 `build` 目录结构：
- `build/index.html` - 中文首页
- `build/en/index.html` - 英文首页

### 3. 本地预览构建结果

```bash
npm run serve
```

---

## ⚠️ 注意事项

1. **GitHub Pages 配置**
   - 如果不配置自定义域名，当前的 `baseUrl: '/'` 配置会导致 GitHub Pages 部署失败
   - 必须选择上述三个选项之一

2. **链接更新**
   - 所有内部链接会自动更新（Docusaurus 会处理）
   - 外部分享的链接需要更新

3. **SEO 影响**
   - URL 变化可能影响搜索引擎索引
   - 建议配置 301 重定向（如果有旧链接）

---

## 📝 推荐方案

**如果你有域名**: 使用选项 1（自定义域名）
- ✅ 专业的访问地址
- ✅ 更好的 SEO
- ✅ 更灵活的配置

**如果没有域名**: 使用选项 2（用户页面）或恢复原配置
- 选项 2: 适合个人博客主站
- 恢复原配置: 适合项目文档

---

## 🔧 当前状态

- ✅ 配置已更新为 `baseUrl: '/'`
- ⏳ 等待你选择部署方式
- ⏳ 需要推送代码到 GitHub

---

**请告诉我你想使用哪种部署方式，我会帮你完成相应的配置！**
