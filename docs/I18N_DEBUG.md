# 🌍 国际化问题诊断和修复

## 问题描述

访问 `http://localhost:3000/en` 显示 404 页面。

## 原因分析

Docusaurus 的博客插件配置了 `routeBasePath: '/'`，这意味着：
- 中文博客首页：`/`
- 英文博客首页：`/en/`

英文版本需要有博客文章才能正常显示。

## 当前状态

### 已有的英文内容

1. **博客文章**（3 篇）
   - `i18n/en/docusaurus-plugin-content-blog/2025/01-15-welcome.md`
   - `i18n/en/docusaurus-plugin-content-blog/2025/01-20-typescript-best-practices.md`
   - `i18n/en/docusaurus-plugin-content-blog/2025/02-01-react-hooks-guide.md`

2. **UI 翻译**
   - `i18n/en/code.json`
   - `i18n/en/docusaurus-theme-classic/navbar.json`
   - `i18n/en/docusaurus-theme-classic/footer.json`
   - `i18n/en/docusaurus-plugin-content-blog/options.json`

### 缺少的内容

- ❌ 没有英文版的自定义页面（已删除以避免重复路由）

## 解决方案

### 步骤 1: 清除缓存

```bash
npm run clear
```

### 步骤 2: 重启开发服务器

```bash
npm start
```

### 步骤 3: 正确访问英文版本

**正确的 URL**：
```
http://localhost:3000/en/
```

**注意**：末尾的 `/` 很重要！

### 步骤 4: 验证访问

访问以下 URL 确认：

#### 中文版本
- 首页：`http://localhost:3000/`
- 归档：`http://localhost:3000/archive`
- 标签云：`http://localhost:3000/tags-cloud`
- 标签：`http://localhost:3000/tags`

#### 英文版本
- 首页：`http://localhost:3000/en/`
- 标签：`http://localhost:3000/en/tags`
- 文章：`http://localhost:3000/en/welcome-to-my-blog`

## 常见问题

### Q1: 为什么 `/en` 不行，必须是 `/en/`？

**A**: Docusaurus 的路由系统要求博客首页路径以 `/` 结尾。

### Q2: 为什么没有英文的归档和标签云页面？

**A**: 为了避免重复路由警告，我们删除了 `i18n/en/docusaurus-plugin-content-pages/` 下的页面。
归档和标签云页面现在是全局的（不区分语言）。

### Q3: 如何添加英文版的自定义页面？

**A**: 如果需要英文版的归档和标签云，需要：

1. 修改中文页面路径：
   ```
   src/pages/archive.tsx → src/pages/zh-CN/archive.tsx
   src/pages/tags-cloud.tsx → src/pages/zh-CN/tags-cloud.tsx
   ```

2. 创建英文页面：
   ```
   i18n/en/docusaurus-plugin-content-pages/archive.tsx
   i18n/en/docusaurus-plugin-content-pages/tags-cloud.tsx
   ```

但这会增加复杂性，目前的方案是使用全局页面（所有语言共享）。

## 验证清单

重启服务器后，请验证：

- [ ] `http://localhost:3000/` - 显示中文博客首页
- [ ] `http://localhost:3000/en/` - 显示英文博客首页
- [ ] 点击语言切换器可以切换语言
- [ ] URL 正确变化（`/` ↔ `/en/`）
- [ ] 英文博客文章可以访问
- [ ] 标签页面正常
- [ ] 归档页面正常
- [ ] 标签云页面正常

## 部署后的 URL

在 GitHub Pages 部署后，URL 会包含 `/blog-v1/`：

### 中文版本
- 首页：`https://pfyangf.github.io/blog-v1/`
- 归档：`https://pfyangf.github.io/blog-v1/archive`

### 英文版本
- 首页：`https://pfyangf.github.io/blog-v1/en/`
- 标签：`https://pfyangf.github.io/blog-v1/en/tags`

## 调试技巧

### 1. 查看生成的路由

开发服务器启动后，查看控制台输出的路由列表。

### 2. 检查 .docusaurus 目录

```bash
ls .docusaurus/docusaurus-plugin-content-blog/default/
```

应该看到中英文的博客数据。

### 3. 查看浏览器控制台

打开浏览器开发者工具，查看是否有 JavaScript 错误。

## 总结

**问题**：访问 `/en` 显示 404

**解决**：
1. 清除缓存：`npm run clear`
2. 重启服务器：`npm start`
3. 访问正确的 URL：`http://localhost:3000/en/`（注意末尾的 `/`）

**原因**：
- 博客配置了 `routeBasePath: '/'`
- Docusaurus 要求博客首页路径以 `/` 结尾
- 缓存可能导致旧的路由信息

---

**现在重启服务器，访问 `http://localhost:3000/en/` 应该可以看到英文博客了！** 🌍
